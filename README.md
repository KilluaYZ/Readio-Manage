# ReadIO 后台管理系统

ReadIO 项目的后台管理系统，基于 Vue 2 和 Element UI 开发，提供完整的内容管理、用户管理、系统监控等功能。

## 📋 项目简介

ReadIO 后台管理系统是一个前后端分离的 Web 应用，为管理员提供便捷的内容管理和系统监控功能。系统基于 RuoYi 框架模板开发，采用 Vue 2 + Element UI 技术栈。

### 主要功能

- 👥 **用户管理**：用户信息查看、权限管理、角色分配
- 📚 **内容管理**：书籍管理、帖子管理、标签管理
- 📁 **文件管理**：电子书文件上传、管理、下载
- 📊 **数据可视化**：数据统计、图表展示
- 🔍 **系统监控**：服务器监控、日志查看、在线用户
- ⚙️ **系统配置**：系统参数配置、菜单管理

## 🛠️ 技术栈

### 核心技术

- **框架**：Vue 2.6.12
- **UI 组件库**：Element UI 2.15.10
- **状态管理**：Vuex 3.6.0
- **路由管理**：Vue Router 3.4.9
- **HTTP 客户端**：Axios 0.24.0
- **构建工具**：Vue CLI 4.4.6
- **CSS 预处理器**：Sass/SCSS

### 主要依赖

```json
{
  "vue": "2.6.12",
  "element-ui": "2.15.10",
  "vuex": "3.6.0",
  "vue-router": "3.4.9",
  "axios": "0.24.0",
  "echarts": "4.9.0",
  "js-cookie": "3.0.1",
  "nprogress": "0.2.0"
}
```

### 开发工具

- **代码规范**：ESLint
- **代码格式化**：Prettier（可选）
- **Git Hooks**：Husky + lint-staged

## 📁 项目结构

```
Readio-Manage/
├── public/                    # 静态资源
│   ├── index.html            # HTML 模板
│   ├── favicon.ico           # 网站图标
│   └── html/                 # 其他 HTML 文件
│
├── src/                       # 源代码目录
│   ├── api/                  # API 接口定义
│   │   ├── login.js          # 登录相关 API
│   │   ├── manage/           # 管理功能 API
│   │   │   ├── file.js       # 文件管理
│   │   │   ├── post.js       # 帖子管理
│   │   │   ├── tag.js        # 标签管理
│   │   │   └── works.js      # 作品管理
│   │   ├── monitor/          # 监控相关 API
│   │   │   ├── server.js     # 服务器监控
│   │   │   ├── online.js     # 在线用户
│   │   │   └── operlog.js    # 操作日志
│   │   └── system/           # 系统管理 API
│   │
│   ├── assets/               # 资源文件
│   │   ├── icons/            # 图标文件
│   │   ├── images/           # 图片资源
│   │   ├── logo/             # Logo 文件
│   │   └── styles/           # 样式文件
│   │       ├── index.scss    # 主样式文件
│   │       ├── variables.scss # 变量定义
│   │       └── element-ui.scss # Element UI 样式覆盖
│   │
│   ├── components/           # 可复用组件
│   │   ├── Breadcrumb/       # 面包屑导航
│   │   ├── DictTag/          # 字典标签
│   │   ├── FileUpload/       # 文件上传
│   │   ├── ImageUpload/      # 图片上传
│   │   ├── Pagination/       # 分页组件
│   │   └── ...
│   │
│   ├── directive/            # 自定义指令
│   │   ├── permission/       # 权限指令
│   │   └── dialog/           # 对话框指令
│   │
│   ├── layout/               # 布局组件
│   │   ├── components/       # 布局子组件
│   │   │   ├── Sidebar/      # 侧边栏
│   │   │   ├── Navbar/       # 导航栏
│   │   │   └── AppMain/      # 主内容区
│   │   └── index.vue        # 布局主文件
│   │
│   ├── router/               # 路由配置
│   │   └── index.js         # 路由定义
│   │
│   ├── store/                # Vuex 状态管理
│   │   ├── modules/          # 状态模块
│   │   │   ├── user.js       # 用户状态
│   │   │   ├── app.js        # 应用状态
│   │   │   └── permission.js # 权限状态
│   │   ├── getters.js        # Getters
│   │   └── index.js          # Store 入口
│   │
│   ├── utils/                # 工具函数
│   │   ├── auth.js           # 认证工具
│   │   ├── request.js        # HTTP 请求封装
│   │   ├── validate.js      # 表单验证
│   │   └── ruoyi.js          # RuoYi 工具函数
│   │
│   ├── views/                # 页面视图
│   │   ├── dashboard/        # 仪表盘
│   │   ├── manage/           # 管理页面
│   │   │   ├── file.vue      # 文件管理
│   │   │   ├── post.vue      # 帖子管理
│   │   │   ├── tag.vue       # 标签管理
│   │   │   └── works.vue     # 作品管理
│   │   ├── monitor/          # 监控页面
│   │   │   ├── server.vue    # 服务器监控
│   │   │   ├── online.vue    # 在线用户
│   │   │   └── operlog.vue   # 操作日志
│   │   ├── system/           # 系统管理
│   │   ├── login.vue         # 登录页面
│   │   └── error/            # 错误页面
│   │
│   ├── App.vue               # 根组件
│   ├── main.js               # 入口文件
│   ├── permission.js         # 权限控制
│   └── settings.js           # 配置文件
│
├── build/                    # 构建脚本
│   └── index.js             # 构建入口
│
├── bin/                      # 脚本文件
│   ├── build.bat            # Windows 构建脚本
│   ├── package.bat          # Windows 打包脚本
│   └── run-web.bat          # Windows 运行脚本
│
├── .eslintrc.js             # ESLint 配置
├── babel.config.js           # Babel 配置
├── vue.config.js             # Vue CLI 配置
├── package.json             # 项目配置
└── README.md                # 项目文档
```

## 🚀 快速开始

### 环境要求

- **Node.js**：>= 8.9
- **npm**：>= 3.0.0
- **Git**：用于版本控制

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd ReadIO/Readio-Manage
```

#### 2. 安装依赖

```bash
# 使用 npm（推荐）
npm install

# 如果下载速度慢，可以使用国内镜像
npm install --registry=https://registry.npmmirror.com

# 或使用 yarn
yarn install
```

**注意**：如果遇到依赖冲突，可以使用 `npm install --force` 强制安装，但不推荐直接使用 `cnpm`。

#### 3. 配置环境变量

在项目根目录创建 `.env.development`（开发环境）和 `.env.production`（生产环境）文件：

```bash
# .env.development
VUE_APP_BASE_API = 'http://localhost:5000'

# .env.production
VUE_APP_BASE_API = 'https://your-api-domain.com'
```

#### 4. 启动开发服务器

```bash
npm run dev
# 或
npm run serve
```

访问：http://localhost:80（默认端口，可在 `vue.config.js` 中修改）

### 构建生产版本

```bash
# 构建生产环境
npm run build:prod

# 构建预发布环境
npm run build:stage
```

构建产物位于 `dist/` 目录。

## 📱 功能模块

### 1. 用户管理

- 用户列表查看和搜索
- 用户信息编辑
- 用户权限管理
- 用户状态管理（启用/禁用）

**相关文件：**
- `src/views/system/user/index.vue`
- `src/api/system/user.js`

### 2. 内容管理

#### 书籍管理
- 书籍列表查看
- 书籍信息编辑
- 书籍状态管理

#### 帖子管理
- 帖子列表查看
- 帖子审核
- 帖子删除

#### 标签管理
- 标签列表
- 标签添加/编辑/删除
- 标签统计

#### 作品管理
- 二创作品列表
- 作品审核
- 作品管理

**相关文件：**
- `src/views/manage/`
- `src/api/manage/`

### 3. 文件管理

- 文件上传
- 文件列表查看
- 文件下载
- 文件删除

**相关文件：**
- `src/views/manage/file.vue`
- `src/api/manage/file.js`

### 4. 系统监控

#### 服务器监控
- CPU 使用率
- 内存使用情况
- 磁盘使用情况
- 系统负载

#### 在线用户
- 在线用户列表
- 用户会话管理
- 强制下线

#### 操作日志
- 操作日志查看
- 日志搜索和筛选
- 日志导出

**相关文件：**
- `src/views/monitor/`
- `src/api/monitor/`

### 5. 系统配置

- 系统参数配置
- 菜单管理
- 角色管理
- 字典管理

**相关文件：**
- `src/views/system/`
- `src/api/system/`

## 🔧 开发指南

### API 请求封装

所有 API 请求通过 `src/utils/request.js` 封装，自动处理：

- Token 认证
- 请求/响应拦截
- 错误处理
- 请求取消

**使用示例：**

```javascript
// src/api/manage/file.js
import request from '@/utils/request'

// GET 请求
export function getFileList(params) {
  return request({
    url: '/manage/file/list',
    method: 'get',
    params
  })
}

// POST 请求
export function uploadFile(data) {
  return request({
    url: '/manage/file/upload',
    method: 'post',
    data
  })
}
```

### 状态管理

使用 Vuex 进行状态管理，主要模块：

- **user**：用户信息、Token
- **app**：应用配置、侧边栏状态
- **permission**：路由权限

**使用示例：**

```javascript
// 在组件中使用
import { mapGetters } from 'vuex'

export default {
  computed: {
    ...mapGetters(['user', 'token'])
  },
  methods: {
    getUserInfo() {
      this.$store.dispatch('user/getInfo')
    }
  }
}
```

### 路由配置

路由配置位于 `src/router/index.js`，支持：

- 动态路由加载
- 路由权限控制
- 路由懒加载

### 权限控制

使用 `v-permission` 指令进行按钮级权限控制：

```vue
<template>
  <el-button v-permission="['manage:file:add']">添加文件</el-button>
</template>
```

### 组件使用

#### 文件上传组件

```vue
<template>
  <FileUpload
    :limit="1"
    :file-size="10"
    :file-type="['pdf', 'epub', 'txt', 'mobi']"
    @success="handleUploadSuccess"
  />
</template>
```

#### 分页组件

```vue
<template>
  <pagination
    v-show="total > 0"
    :total="total"
    :page.sync="queryParams.pageNum"
    :limit.sync="queryParams.pageSize"
    @pagination="getList"
  />
</template>
```

## 🎨 样式开发

### SCSS 变量

在 `src/assets/styles/variables.scss` 中定义全局变量：

```scss
$base-color: #409eff;
$text-color: #303133;
```

### 样式覆盖

Element UI 样式覆盖在 `src/assets/styles/element-ui.scss` 中。

## 🧪 测试

### 代码检查

```bash
npm run lint
```

### 单元测试（可选）

```bash
npm run test:unit
```

## 📦 部署

### 开发环境部署

1. 构建项目：`npm run build:prod`
2. 将 `dist/` 目录部署到 Web 服务器（Nginx、Apache 等）

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend-server:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### Docker 部署（可选）

```dockerfile
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

## 🔍 常见问题

### 1. 依赖安装失败

**问题**：`npm install` 失败或很慢

**解决方案**：
- 使用国内镜像：`npm install --registry=https://registry.npmmirror.com`
- 清除缓存：`npm cache clean --force`
- 删除 `node_modules` 和 `package-lock.json` 后重新安装

### 2. 端口被占用

**问题**：启动时提示端口被占用

**解决方案**：
- 修改 `vue.config.js` 中的 `port` 配置
- 或使用 `npm run dev -- --port 8080` 指定端口

### 3. 跨域问题

**问题**：开发环境请求 API 时出现跨域错误

**解决方案**：
- 在 `vue.config.js` 中配置代理
- 或让后端服务配置 CORS

## 📝 开发规范

### 代码风格

- 遵循 ESLint 配置的代码规范
- 使用 2 空格缩进
- 使用单引号
- 组件名使用 PascalCase
- 文件名使用 kebab-case

### Git 提交规范

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

## 📚 参考文档

- [Vue 2 官方文档](https://v2.cn.vuejs.org/)
- [Element UI 文档](https://element.eleme.cn/)
- [Vue Router 文档](https://router.vuejs.org/zh/)
- [Vuex 文档](https://vuex.vuejs.org/zh/)
- [RuoYi 文档](http://doc.ruoyi.vip/ruoyi/)

## 📄 许可证

本项目采用 [MIT 许可证](LICENSE)。

Copyright (c) 2023 ReadIO

详情请参阅 [LICENSE](LICENSE) 文件。

## 📮 联系方式

如有问题或建议，请提交 Issue 或联系开发团队。

---

**ReadIO 后台管理系统** - 让管理更简单
